{% extends 'layout.html.twig' %}

{% block title %}Les catégories{% endblock %}

{% block content %}
    <h1>Titres des catégories</h1>
    <ul>
        {% for category in categories %}
            <li>{{ category.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}